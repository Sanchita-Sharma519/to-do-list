<html>
    <head>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js" integrity="sha512-3gJwYpMe3QewGELv8k/BX9vcqhryRdzRMxVfq6ngyWXwo03GFEzjsUm8Q7RZcHPHksttq7/GFoxjCVUjkjvPdw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

<!-- jQuery library -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.4/jquery.min.js"></script>

<!-- Latest compiled JavaScript -->
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
<link rel="stylesheet" type="text/css"  href="../css/userPage.css">
    </head>
    <body>
        <h1>Welcome <%= user.name %></h1>
        <div class="outer">
        <ul>
        <%  for (let listitem of user.list){ %>
            <li>
              <div class="list">
                <b><%= listitem.name %></b>
                <b><%= listitem.id %></b>
                <form action="/delete/<%= listitem.id %>/<%= user.id %>" method="POST">
                  <button type="submit" id="delete-task-button" class="btn btn-primary">delete</button>
                </form>
              </div>
            </li>
            <% } %>
        </ul>
        </div>
        <h1 style="align-items: center;justify-content: center;">Add Task</h1>
        <form action="/addTask/<%= user.id %>" method="POST">
            <div class="form-group">
              <label for="name">name</label>
              <input type="text" class="form-control" id="name" name="name" placeholder="Enter name">
            </div>
            <div class="form-group">
                <label for="description">description</label>
                <input type="text" class="form-control" id="description" name="description" placeholder="Enter description">
              </div>
            <div class="form-group">
              <label for="date">Date</label>
              <input type="date" class="form-control" id="date" name="date" placeholder="date">
            </div>
            <button type="submit" id="add-task-button" class="btn btn-primary">Submit</button>
          </form>

    </body>
    <script>
        
            sessionStorage.setItem(user,locals.user)
            console.log('<%= user %>')
        document.getElementById('add-task-button').onsubmit=async(event)=>{
            event.preventDefault();
            await $.ajax({
        type: 'get',
        url: '/user/lesson/getLessonIds',
        data: {},
        success: async (result, status, xhr) => {

        },
        error: (xhr, status, err) => {

        }
    });
}
    </script>
</html>